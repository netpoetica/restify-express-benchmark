{
  "name": "restify-express-benchmark",
  "version": "1.0.0",
  "description": "Using Apache Bench to load test these two frameworks",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "restify",
    "benchmark"
  ],
  "author": "netpoetica",
  "license": "MIT",
  "dependencies": {
    "express": "^4.10.7",
    "restify": "^8.4.0"
  }
}
